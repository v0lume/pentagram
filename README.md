
## Set-up

 - install docker
 - exec `docker-compose build --no-cache`
 - update your own `/etc/hosts` file
 - start application using `docker-compose up -d`

## Hosts

```
127.0.0.1 ng.pentagram api.ng.pentagram